---
description: Full verification gate — run tests then production build
---

Execute the verification gate: `npm test && npm run build`. If either step fails, diagnose and fix before declaring done.
